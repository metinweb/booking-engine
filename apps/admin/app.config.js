// Application-specific frontend configuration
// This file can be used to store settings like API base URLs, feature flags, etc.

const config = {
  // Other frontend configurations can go here
  // featureFlags: {
  //   newDashboard: true,
  // },
}

export default config
